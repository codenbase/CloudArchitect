一台服务器在日常平稳运营期，最多能养活多少用户？当海量流量打过来，CPU、内存、磁盘和网卡，谁会第一个倒下？

本文选用大家都熟悉的**内容平台**（如掘金、知乎或微博）作为业务模型，来设计一系列实验，通过计算来回答这个问题。
## 环境

**被测服务器**：
配置：4C8G。
架构：Go Web 服务 和 MySQL 部署在同一台服务器上。

**施压机**：
配置：2C4G。
压测工具：k6。

**为什么必须在 VPC 内网做压测？** 我们的目标是“计算一台服务器能扛住多少流量？最先达到什么瓶颈？” 即单机性能极压测。

**为什么不用本地电脑做施压机？** 家庭上行宽带限制，路由器连接数限制，云厂商防御（防 DDoS 攻击）机制，公网带宽限制（无法看到 CPU 或 磁盘 的真正死法）。

**内网压测价值**：精确测出 CPU 算力，磁盘 IOPS，指导未来如何扩容（加机器还是加缓存）。

**关于 P99**：在 VPC 内网测的是基准 P99，排除网络干扰，看纯服务器处理业务和数据库的性能，如果内网 P99 都超过了 500ms，则代码和数据库绝对有性能瓶颈，必须优化。而真实的公网 P99，从来不是靠“压测”测出来的，而是服务上线后，通过接入层（Nginx/网关）的访问日志统计，或者前端/客户端的 APM（应用性能监控）上报得出的。因此，**压测找系统瓶颈（走内网），监控看用户体验（走公网）。**

先给这台机器做个“全身体检”，摸清它的物理底牌。
1. 使用 `lscpu` 命令查看 CPU 架构、型号和 L3 缓存大小。L3 缓存对数据库性能影响极大。
2. 查看云厂商超卖现象。`top` 后按 `1`  观察 `%st` ，经常大于 0 表示“邻居”在疯狂抢占 CPU。
3. 测试磁盘的基础 I/O 能力。
4. 测试网络带宽。
5. `ulimit -n` 查看单进程最大文件描述符限制。
6. 查看系统默认的 TCP 半连接队列和全连接队列大小。

## 实验一：海量用户同时登录

这是一场对 CPU 算力的考验。在登录场景下，拖垮服务器的往往不是查数据库，而是复杂的数学运算。

登录场景，是纯粹的**计算密集型**任务。适用于所有“安全接入”的场景，如物联网设备接入，底层技术栈高度一致。

**登录性能消耗点：**
1. HTTPS（TLS 握手）：建立安全通道的非对称加密计算。
2. 密码哈希比对：使用 bcrypt、Argon2 或 PBKDF2 等慢速哈希算法，对密码加盐哈希，并与数据库密文比对。
3. 令牌签发：生成 SHA256 签名，JWT 序列化。

**压力测试（可能的瓶颈）：**
1. 第一波冲击：TCP/IP 与 TLS 协议栈的算力消耗，多核 CPU 的使用率开始急剧攀升。
2. 第二波冲击：慢哈希（bcrypt 算法）的“算力陷阱”，假设配置的 `cost` 参数为 10，单次哈希耗时可能达到 50 毫秒，一台 4 核服务器，每秒最多只能提供 4000 毫秒的 CPU 时间，那么这台机器在物理极限下，每秒最多只能处理 80 个登录请求（QPS = 80）。
3. 最终崩溃形态：排队与上下文切换灾难：当并发量达到 500 时，CPU 只有 4 个核心，剩下的 496 个请求去哪了？它们只能在 Go 的协程队列里排队等待。
	1. **现象 1：CPU 飙红。** `top` 命令下，`us`（用户态 CPU）死死卡在 100%，系统平均负载（Load Average）飙升到机器核心数的数倍甚至数十倍。
	2. **现象 2：RT 雪崩。** 吞吐量（QPS）不再增加，但 P99 响应时间从正常的 50ms 暴涨到 5000ms 甚至触发网关超时（504 Gateway Timeout）。
	3. **现象 3：数据库“闲得发慌”。** 此时查看 MySQL 状态，你会发现连接数很少，磁盘 I/O 几乎为零。因为应用层根本没有足够的算力把请求组装成 SQL 发给数据库。

**开发流程：**
我们正在初始化一个名为 `monolith-stress-lab` 的性能压测项目。这个项目将 example/monolith-stress-lab/ 作为根目录，你生成的所有代码都必须在这个文件夹下面。请帮我生成以下基础项目骨架：
**Go Web 服务端：**
- 初始化 `go.mod`。包名是：github.com/codenbase/kubernetes/example/monolith-stress-lab。
- 编写 `main.go`，启动一个基于 `gin` 的 Web 服务，监听 8080 端口。
- 提供一个 `POST /login` 接口。接收 JSON `{"username": "xxx", "password": "xxx"}`。
- 接口内部需要连接 MySQL，根据用户名查出 `password_hash`，并使用 `golang.org/x/crypto/bcrypt` 进行哈希比对。
- 比对成功后，生成一个简单的 JWT 字符串返回。
- 必须从环境变量读取数据库连接 DSN 和 bcrypt 的 Cost 值（默认设为 10）。
**数据库基建 (`/db` 目录):**
- 编写一个 `docker-compose.yml`，用于快速拉起 MySQL 8.0。
- 编写一个 `init.sql`，包含 `users` 表的创建语句，并自动插入 1 万条测试用户数据（密码统一为 `123456` 的 bcrypt 哈希值）。
**压测脚本 (`/loadtest` 目录):**
- 编写一个基础的 `k6` 压测脚本 `login_test.js`，向 `http://localhost:8080/login` 持续发送包含随机现有用户名的 POST 请求。

请制定 Plan，由我确认。

**调优方法：**
1. 协议层优化：复用与降级。 开启 HTTP Keep-Alive & TLS 卸载（需引入网关）。
2. 应用层优化：哈希成本与并行度。动态调整 Hash Cost （牺牲一点安全性）& 优化并发模型（Go 合理设置 `GOMAXPROCS`）。
3. 业务优化：黑名单缓存限制高频非法登录 & 增加 JWT 令牌有效时间（有效期免登录）。

**慎用免登录场景（安全性和性能永远是互斥的）：**
1. 核心资产高危操作，如修改密码，银行卡相关，VPP 下发电网指令等。
2. 防重放攻击，云端向一台智能汽车发送“解锁车门”的指令。凭证决不能复用。
3. 完全无状态的 RESTful 开放 API。对于面向第三方开发者的 Open API 网关，为了极端的解耦，有时不维护任何会话状态。每一次 API 调用，调用方都必须使用 AppKey 和 AppSecret 对整个请求体进行高强度的 HMAC 签名。服务端每次都要重新计算整个 Body 的签名来比对。请求量越大，CPU 越惨。

## 实验二：高频浏览热门文章

这考验的是I/O与网络带宽的承载能力。
1. 文章内容很大：网络带宽瞬间打满？
2. 文章内容很小：Web服务疯狂序列化？内存压力飙升？数据库连接池被耗尽？

干货：用 `dstat` 或 `nload` 看网络带宽，用 `netstat` 看 TCP 连接数。

## 实验三：海量用户同时发帖/评论

磁盘 I/O 会最先倒下？

干货：看 `iostat` 中的 `%util`（磁盘使用率）和 `await`（I/O等待时间）。

## 单机极限调优

1. 调整 Web 框架的协程池大小
2. 优化慢查询索引
3. 调整数据库连接池大小，解释为什么连接池不是越大越好？
4. 引入 Redis 缓存

## 终于扛不住了

1. 读写分离与动静分离，数据库单独一台服务器，静态资源CDN，让原本的单机专门跑 Web 服务
2. 引入消息队列，应对并发写
3. 走向云原生...

